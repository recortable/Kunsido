<% if comment.id %>
    <%= div_for comment do %>
        <%= image_tag avatar_url(comment.user, 16) %> <%= comment.user.name %> dijo hace <%= time_ago_in_words comment.created_at %>
        [<%= link_to 'editar', [:edit, comment] %>
        <%= link_to 'borrar', comment, :method => :delete, :confirm => '¿Deseas borrar este comentario?' %>]
        <%= render_body comment %>
    <% end %>
<% end %>